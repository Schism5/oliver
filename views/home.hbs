<!DOCTYPE html>
<html>
    <head>
       <meta charset="utf-8">
       <title>Home</title>
    </head>
    <body>
        <h1>{{ title }}</h1>
        <p>{{ welcomeMessage }}</p>

    <ul>
        {{#each imageNames }}
            <img src="/images/{{this}}" alt="">
        {{/each }}
    </ul>

        <footer>
            <p>Copyright {{ currentYear }}</p>
        </footer>
    </body>
</html>